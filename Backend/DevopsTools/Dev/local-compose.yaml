services:
  # --------- Envoy ---------
  backend-gateway:
    image: envoyproxy/envoy:${ENVOY_TAG:-latest}
    container_name: backend-gateway
    ports:
      - "${GATEWAY_PORT}:443"
      - "9901:9901"
    networks:
      - local-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: [ "envoy", "-c", "/etc/envoy/envoy.yaml" ]
    volumes:
      - ./envoy/backend-gateway.local.yaml:/etc/envoy/envoy.yaml
      - ./certs/backend-gateway:/etc/envoy/certificates
  
networks:
  local-network:
    driver: bridge

