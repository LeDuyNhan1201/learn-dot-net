Learn Dot Net has been rendered in namespace {{ include "learn-dot-net.namespace" . }}.

The API gateway expects this certificate secret:
  {{ include "learn-dot-net.apiGatewaySecretName" . }}

Port-forward the gateway with:
  kubectl -n {{ include "learn-dot-net.namespace" . }} port-forward svc/{{ include "learn-dot-net.apiGatewayName" . }} {{ .Values.apiGateway.externalPort }}:{{ .Values.apiGateway.service.httpsPort }}

Test through Envoy with:
  curl -k --resolve {{ .Values.apiGateway.host }}:{{ .Values.apiGateway.externalPort }}:127.0.0.1 https://{{ .Values.apiGateway.host }}:{{ .Values.apiGateway.externalPort }}/health/info
